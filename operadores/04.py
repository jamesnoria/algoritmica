# Algoritmo que convierta horas en minutos y en segundos

horas = int(input('Inserte la hora a convertir: '))
minutos = horas * 60
segs = horas * 3600
print(f'{horas} -> MIN: {minutos} -> SEG: {segs}')